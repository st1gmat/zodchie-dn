services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: zodchie
      POSTGRES_PASSWORD: zodchie
      POSTGRES_DB: zodchie
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
